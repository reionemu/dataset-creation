# Dataset Creation

Parameter sampling, simulation runs, and dataset assembly for reionemu training data, with Bridges-2 CPU and GPU pipelines.

The emulator package is maintained at [reionemu/reionemu](https://github.com/reionemu/reionemu).

## Data

Only metadata is tracked: parameter files, run logs, and notes. Raw and processed HDF5 output is not committed and is archived separately.
